version: '3.4'

services:

  django:
    restart: always
    container_name: django
    env_file:
      - ./django/mainapp/.env
    build:
      context: ./django/
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    
    networks:
      - proxy_net

  frontend:
    restart: always
    build: 
      context: ./myapp/
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "3000:3000"
    stdin_open: true
    depends_on:
      - django
    networks:
      - proxy_net

networks:
  proxy_net:
    external: true